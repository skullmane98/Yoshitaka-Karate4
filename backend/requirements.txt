# Yoshitaka Karate-Do CMS — backend dependencies
# Loose pins so the same file builds on Python 3.11–3.14 (Render keeps changing defaults).

# Web framework
fastapi>=0.110,<0.120
uvicorn[standard]>=0.25,<1.0
starlette>=0.37,<1.0
python-multipart>=0.0.9

# Validation
pydantic>=2.10,<3.0
email-validator>=2.0

# Auth / crypto
PyJWT>=2.8,<3.0
bcrypt>=4.0,<5.0

# Database (MySQL via SQLModel/SQLAlchemy async)
SQLAlchemy>=2.0,<2.1
sqlmodel>=0.0.22
aiomysql>=0.2.0
PyMySQL>=1.1.0
cryptography>=40.0
greenlet>=3.0

# QR / barcode generation for member certificates
qrcode>=8.0
python-barcode>=0.16
Pillow>=10.0

# Email reminders (SMTP)
aiosmtplib>=3.0

# Misc
python-dotenv>=1.0
httpx>=0.27
authlib>=1.3
itsdangerous>=2.2
